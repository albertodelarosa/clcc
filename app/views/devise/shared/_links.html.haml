.hidden-xs.hidden-sm.btn-group.btn-group-justified{"role" => "group"}
  - if controller_name != 'sessions'
    = link_to "log in", new_session_path(resource_name), class: "btn btn-default"
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "register", new_registration_path(resource_name), class: "btn btn-default"
%br/
.hidden-xs.hidden-sm.btn-group.btn-group-justified{"role" => "group"}
  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "forgot password?", new_password_path(resource_name), class: "btn btn-default"
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "resend confirmation", new_confirmation_path(resource_name), class: "btn btn-default"
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = link_to "resend unlock", new_unlock_path(resource_name), class: "btn btn-default"
.visible-xs-block.visible-sm-block.btn-group-sm.btn-group-vertical{"role" => "group"}
  - if controller_name != 'sessions'
    = link_to "log in", new_session_path(resource_name), class: "btn btn-default"
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to "register", new_registration_path(resource_name), class: "btn btn-default"
  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    = link_to "forgot password?", new_password_path(resource_name), class: "btn btn-default"
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    = link_to "resend confirmation", new_confirmation_path(resource_name), class: "btn btn-default"
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    = link_to "resend unlock", new_unlock_path(resource_name), class: "btn btn-primary"

